import { React } from 'react';

import '../Header/Header.css';

const header = () => {
    return (
        <header className="header">
            Header
        </header>
    );
}

export default header;